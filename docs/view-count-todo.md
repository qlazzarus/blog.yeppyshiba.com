# 조회수 집계 운영 TODO

완료한 설계·이전 절차는 `workers/view-counter/README.md`와 migration에 남긴다. 이 문서는 실제 운영 전환에 남은 일만 관리한다.

- [ ] 실제 브라우저에서 `POST /v1/views`의 CORS·HttpOnly 쿠키 발급과 같은 글의 UTC 일별 중복 제한을 확인한다.
- [ ] Worker 오류·차단·D1 오류의 관측 방법을 정하고, 개인 데이터 없이 이상 급증을 확인할 수 있게 한다.
- [ ] 2~4주 동안 GA4와 자체 집계의 추이를 비교한다.
- [ ] 검증 뒤 UI 라벨과 개인정보처리방침을 익명 쿠키 기반 집계에 맞춘다.
- [ ] 위 전환이 끝나면 `ga-views.json` 표시 fallback과 `ga:fetch`의 조회수 표시 용도를 제거한다. API 실패 시에는 `—`를 표시한다.
- [ ] D1 백업·일별 중복 방지 데이터 보존 기간·월 비용 알림을 설정한다.
- [ ] URL 변경 이력이 생기면 baseline alias가 필요한지 확인한다.
